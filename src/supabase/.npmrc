# DENO ENVIRONMENT - NOT FOR NPM
# This folder contains Supabase Edge Functions that run in Deno runtime
# Import syntax like npm:package and jsr:@scope/package is ONLY for Deno
# NPM should completely ignore this directory

registry=https://deno.land/
audit=false
fund=false